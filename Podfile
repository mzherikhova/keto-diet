# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'KetoDiet' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for KetoDiet
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Storage'
  pod 'FirebaseAuth'
  pod 'ARSLineProgress'
  pod 'Locksmith'  

  target 'KetoDietTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'KetoDietUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
